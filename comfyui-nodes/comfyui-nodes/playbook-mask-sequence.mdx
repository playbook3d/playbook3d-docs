---
title: "Mask Pass Sequence"
description: "Retrieves a sequence of Mask Render Pass images from Playbook3D API."
---

## Overview

The **Mask Pass Sequence** node fetches a ZIP file containing multiple **Mask Render Pass** images from Playbook3D. It extracts and returns both the composite mask and 8 individual color-based masks as a batch.

## 🛠️ Node Configuration

### **Inputs**
| Name         | Type   | Required | Default | Description |
|-------------|--------|----------|---------|--------------------------------|
| `api_key`   | STRING | ✅        | N/A     | API key for authentication. |
| `blur_size` | FLOAT  | ✅        | `0.0`   | Gaussian blur radius (0 for no blur). |
| `run_id`    | STRING | ❌        | N/A     | Optional identifier for a specific run. |

### **Outputs**
| Name       | Type   | Description |
|------------|--------|--------------------------------|
| `mask_pass` | IMAGE  | Composite mask image. |
| `mask_1`   | MASK   | Mask extracted from color code #ffe906. |
| `mask_2`   | MASK   | Mask extracted from color code #0589d6. |
| `mask_3`   | MASK   | Mask extracted from color code #a2d4d5. |
| `mask_4`   | MASK   | Mask extracted from color code #000016. |
| `mask_5`   | MASK   | Mask extracted from color code #00ad58. |
| `mask_6`   | MASK   | Mask extracted from color code #f084cf. |
| `mask_7`   | MASK   | Mask extracted from color code #ee9e3e. |
| `mask_8`   | MASK   | Mask extracted from color code #e6000c. |

## ⚙️ Functionality

- Uses `api_key` to authenticate with Playbook3D.
- Retrieves a ZIP file containing multiple **Mask Render Pass** images.
- Extracts **8 individual masks** from the composite image based on predefined color codes.
- Applies optional **Gaussian blur** to smooth mask edges.
- Returns the composite mask and 8 separate masks as a batch.
