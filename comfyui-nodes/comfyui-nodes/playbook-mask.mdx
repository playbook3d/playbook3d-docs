---
title: "Playbook Mask"
description: "Retrieves the Mask Render Pass from Playbook3D API."
---

## Overview

The **Playbook Mask** node fetches the **Mask Render Pass** from Playbook3D using an API key and an optional `run_id`. It applies optional Gaussian blur and returns both the composite mask and individual color-based masks.

## 🛠️ Node Configuration

### **Inputs**
| Name         | Type   | Required | Default | Description |
|-------------|--------|----------|---------|--------------------------------|
| `api_key`   | STRING | ✅        | N/A     | API key for authentication. |
| `blur_size` | FLOAT  | ✅        | `0.0`   | Gaussian blur radius (0 for no blur). |
| `run_id`    | STRING | ❌        | N/A     | Optional identifier for a specific run. |
| `default_value` | IMAGE | ❌     | N/A     | Default mask if no valid render is found. |

### **Outputs**
| Name       | Type   | Description |
|------------|--------|--------------------------------|
| `image`    | IMAGE  | Composite mask image. |
| `mask_1`   | MASK   | Mask extracted from color code #ffe906. |
| `mask_2`   | MASK   | Mask extracted from color code #0589d6. |
| `mask_3`   | MASK   | Mask extracted from color code #a2d4d5. |
| `mask_4`   | MASK   | Mask extracted from color code #000016. |
| `mask_5`   | MASK   | Mask extracted from color code #00ad58. |
| `mask_6`   | MASK   | Mask extracted from color code #f084cf. |
| `mask_7`   | MASK   | Mask extracted from color code #ee9e3e. |
| `mask_8`   | MASK   | Mask extracted from color code #e6000c. |

## ⚙️ Functionality

- Uses `api_key` to authenticate with Playbook3D.
- If `run_id` is provided, fetches the Mask Pass for that specific render.
- If no valid render is found, returns the **default mask**.
- Extracts **8 color-based masks** from the composite image.
- Applies optional **Gaussian blur** to smooth the masks.
