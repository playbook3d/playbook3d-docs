---
title: "Playbook Mask"
description: "Retrieves the Mask Render Pass from Playbook3D API, supporting both single-frame and sequence modes."
---

# Playbook Mask Pass Node

The **Playbook Mask Pass** node retrieves a **single Mask Render Pass frame** from Playbook3D. It extracts and returns both the composite mask and 8 individual color-based masks.

## 🛠️ Node Configuration

### **Inputs**
| Name         | Type   | Required | Default  | Description |
|-------------|--------|----------|----------|--------------------------------|
| `api_key`   | STRING | ✅        | N/A      | API key for authentication. |
| `blur_size` | FLOAT  | ✅        | `0.0`    | Gaussian blur radius (0 for no blur). |
| `run_id`    | STRING | ❌        | N/A      | Optional identifier for a specific run. |

### **Outputs**
| Name       | Type   | Description |
|------------|--------|--------------------------------|
| `mask_pass` | IMAGE  | Composite mask image. |
| `mask_1`   | MASK   | Mask extracted from color code #ffe906. |
| `mask_2`   | MASK   | Mask extracted from color code #0589d6. |
| `mask_3`   | MASK   | Mask extracted from color code #a2d4d5. |
| `mask_4`   | MASK   | Mask extracted from color code #000016. |
| `mask_5`   | MASK   | Mask extracted from color code #00ad58. |
| `mask_6`   | MASK   | Mask extracted from color code #f084cf. |
| `mask_7`   | MASK   | Mask extracted from color code #ee9e3e. |
| `mask_8`   | MASK   | Mask extracted from color code #e6000c. |

## ⚙️ Functionality

- Uses `api_key` to authenticate with Playbook3D.
- Fetches the composite mask and extracts **8 color-based masks**.
- Applies optional **Gaussian blur** to smooth mask edges.

# Playbook Mask Pass Sequence Node

The **Playbook Mask Pass Sequence** node retrieves a **sequence of Mask Render Pass frames** from Playbook3D. It fetches a ZIP file containing multiple mask images and processes them.

## 🛠️ Node Configuration

### **Inputs**
| Name         | Type   | Required | Default  | Description |
|-------------|--------|----------|----------|--------------------------------|
| `api_key`   | STRING | ✅        | N/A      | API key for authentication. |
| `blur_size` | FLOAT  | ✅        | `0.0`    | Gaussian blur radius (0 for no blur). |
| `run_id`    | STRING | ❌        | N/A      | Optional identifier for a specific run. |

### **Outputs**
| Name     | Type   | Description |
|----------|--------|--------------------------------|
| `Images` | IMAGE  | A batch tensor containing extracted Mask Pass frames. |

## ⚙️ Functionality

- Uses `api_key` to authenticate with Playbook3D.
- Retrieves a **ZIP file containing multiple Mask Pass frames**.
- Extracts and processes the masks from the ZIP file.
