---
title: "Playbook Render Result"
description: "Uploads and retrieves rendered images via Playbook3D API."
---

## Overview

The **Playbook Render Result** node allows users to upload and retrieve rendered image results using the Playbook3D API. It authenticates via an API key and uploads the image to generate a downloadable URL.

## 🛠️ Node Configuration

### **Inputs**
| Name     | Type   | Required | Description |
|----------|--------|----------|--------------------------------|
| `images` | IMAGE  | ✅        | The image tensor to be uploaded. |
| `api_key` | STRING | ✅        | API key for authentication. |

### **Outputs**
| Name  | Type   | Description |
|-------|--------|--------------------------------|
| `URL` | STRING | The URL to download the uploaded image. |

## ⚙️ Functionality

- Uses `api_key` to authenticate with Playbook3D.
- Uploads image data to the Playbook3D asset storage.
- Retrieves a downloadable **result URL** upon successful upload.
- Performs request validation and error handling for API key and upload failures.
